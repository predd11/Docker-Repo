name: fifthworkflow
on:
 push:
  branches:
    - master
jobs:
  myjob1:
   runs-on: ubuntu-latest
   steps:
      - name: this is my name
        run: echo "My Name is ${{vars.NAME}}"
      - name: mypasswrod is
        run: echo "My passwrod is ${{secrets.PASSWORD}}"
